apiVersion: v1
kind: Service
metadata:
  name: simple-web-service
  labels:
    app: simple-web
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: simple-web
